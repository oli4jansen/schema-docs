# schema-docs

## Installation

1. Copy over `workflow.yml` to `/.github/workflows/`

...

## Development

`python -m venv venv`

`source ./venv/bin/activate`

`pip install -r requirements.txt`

`python main.py`
